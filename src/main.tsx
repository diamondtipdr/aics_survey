import React from 'react';
import ReactDOM from 'react-dom/client';
import AICSScorecard from './components/aics_lead_magnet';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AICSScorecard />
  </React.StrictMode>
);
